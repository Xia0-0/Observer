# 观察者开发指南

## 项目定位

观察者是一个个人市场观察工具：记录市场环境、回看环境变化，并以交易纪律辅助决策。它不是自动交易或喊单产品。

## 产品原则

- 先观察和记录，再决定是否交易。
- 趋势行情顺势交易；震荡行情低吸高抛；环境不明确时观察、不交易。
- 第一版不接实时行情、不做自动交易、不做喊单。
- 第一版不需要后端和登录；优先简单、可靠的本地使用体验。
- 优先扩展现有代码和组件；没有明确收益时不要重构。

## 技术栈

- React、TypeScript、Vite。
- React Router 负责前端路由。
- React 本地状态管理应用数据；`localStorage` 持久化。
- 原生 CSS，不引入组件库或状态管理库，除非需求确有必要。

## 项目结构

- `src/App.tsx`：路由、应用状态与观察记录的新增、编辑、删除、设为当前操作。
- `src/types.ts`：领域数据类型。
- `src/storage.ts`、`src/seed.ts`：本地存储与初始样例数据。
- `src/strategy.ts`：环境标签和策略纪律文案。
- `src/components/`：可复用布局和表单。
- `src/pages/`：观察面板、环境时间线、观察记录页面。
- `src/index.css`：全局样式。

## 开发规范

- 使用中文面向用户；字段名称和策略文案应清晰、克制、可执行。
- 保持 `Observation` 数据模型简洁，新增字段前先确认它服务于记录、回看或决策纪律。
- 页面通过 props 使用 `App.tsx` 提供的操作，避免引入隐式全局状态。
- 修改持久化格式时，兼顾已有 `localStorage` 数据的读取与降级。
- 每次改动至少运行 `npm run lint` 和 `npm run build`。

## 不要随意修改

- 不改变三种行情环境及其基本纪律：趋势/顺势，震荡/低吸高抛，不明确/观察不交易。
- 不加入自动交易、交易信号、实时行情、后端或登录，除非需求明确要求。
- 不为替换技术栈、引入大型依赖或纯代码美化而重构。
- 不删除或重置用户本地观察记录；涉及破坏性操作时必须提供明确确认或备份路径。

## 当前 MVP 范围

- 新建、编辑、删除市场观察：日期、标的、资金环境、行情环境、市场结构、关键位置、备注。
- 在本地持久化观察记录，并选择一条记录作为当前市场状态。
- 按时间回看观察记录和行情环境切换。
- 根据当前行情环境展示对应的交易策略纪律。
